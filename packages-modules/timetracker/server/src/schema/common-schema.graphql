

type Tag {
  id: ID!
  name: String
}




enum StartYearWeekType {
  FIRST_FOURDAY_WEEK
  FIRST_FULL_WEEK
  FIRST_DAY_WEEK
}


type Task {
  id: String
  name: String
}

extend type Query {
  getTags: [Tag]
  getSettings: Settings
}