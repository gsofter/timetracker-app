

extend enum ConfigCollectionName{
    timetrackers
}

extend enum ConfigurationContributionNames {
    timeTracker
}

"""
 Subscription event for timer
"""
enum TimeRecordPubSubEvents {
    TimeRecordCreated
    TimeRecordUpdated
    TimeRecordDeleted
    TimeRecordConfigurationUpdate
}
